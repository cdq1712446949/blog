<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cdq.blog.dao.UserDao">

    <select id="queryUserByUserName" resultType="com.cdq.blog.model.User">
        SELECT
        user_id,
        nick_name,
        user_phone,
        user_sex,
        user_role,
        user_exprience,
        user_email,
        user_birthday,
        user_head_photo,
        user_desc,
        user_status
        from user
        where  (user_name=#{userName}
        or user_phone=#{userName}
        or user_email=#{userName})
        and pass_word=#{passWord}
    </select>
</mapper>